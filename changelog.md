# 幻昼更新日志
## 1.3.1 内测版本
2026-08-25 内测发布

【重要安装提醒】
本次 1.3.1 与 1.3 签名相同，可直接覆盖安装。如果从 1.2 或更早版本升级，请先在旧版本中导出完整备份，再卸载旧版本并安装 1.3.1，最后导入备份恢复数据。未提前备份可能导致角色、聊天记录、记忆和设置丢失。

## 重点优化：应用更流畅
我们对应用底层做了大幅重构——把拥挤的主程序文件（14412 行）拆分成 11 个功能明确的独立模块（共 12835 行）。这样应用启动更快、操作更流畅，未来更新也更稳定。

## 新增功能

### Live2D 桌面宠物
- 桌面悬浮窗支持 Live2D 模型渲染（小恶魔模型，6 种表情）
- 桌宠可响应触屏事件（虚拟/有线/无线 ADB 触控）
- 桌宠支持语音控制、情绪识别、AI 对话桥接
- 视线跟随双权限方案：
  - Root 设备：getevent 全屏跟随
  - 非 Root 设备：无线调试配对（6 位配对码）+ shell:getevent 视线跟随
- 桌宠表情管理（多通道情绪引擎 + 缓动过渡 + 本地兜底）
- 桌宠便签（设置中编辑，桌宠随机展示一条）
- QQ OneBot 桥接：可通过 NapCat 协议连接 QQ，桌宠代理收发消息

### Memory 向量记忆系统
- 聊天时 AI 能更准确地回忆相关历史内容
- 长对话场景下回答更连贯、更贴合上下文
- 记忆搜索阈值可调，按 bm25 相关度排序

### 多账号支持 + 旧数据迁移
- 支持多个用户账号切换，账号数据自动隔离存储
- 旧版本数据可一键迁移到新账号

### 外部 API 服务器（F24）
- 幻昼可作为 HTTP 服务器对外暴露 API

### 微信集成（F25）
- 多选转发 + 微信协议桥接支持

### 音乐播放（F35）
- 内置音乐播放器

### Token 用量统计（F33）
- 设置中查看每个模型/API 的 token 消耗

### 三套桌面图标（F23）
- 默认 / 暗色 / 粉色（设置中切换）

## 功能改进

### F12 视频 AI 读帧升级
- 短片（< 30 秒）：3 帧（开头/中/末）
- 中片（30 秒 ~ 3 分钟）：5 帧
- 长片（> 3 分钟）：6 帧（开头 1 + 中间均分 4 + 末尾 1）
- 聊天设置可切换单帧模式/多帧模式

### 三种上下文模式重构（F26）
- 抽象 ContextMode，UI 切换更直观

### 记忆延迟入库（F27）
- 聊天记录异步延迟入库，应用更流畅

### 全天候主动消息（F28）
- 后台调度策略优化

### 聊天记录自动备份（F30）
- 定时备份到本地

### 碎片合并 + 污染清洗（F31）
- 记忆后处理

### 实验真流式 + Agent 事件管线
- 聊天设置 → 实验真流式输出开启后边生成边显示
- Agent 工具调用事件可视化

## 性能与稳定性
- 主程序文件精简 67.1%（14412 → 4741 行）
- 后台消息接收更及时
- Android 15/16 兼容：前台服务类型规范
- 新增内容过滤层（ContentFilter）
- 完整备份归档（AppBackupCodec + FullBackupArchive）
- MCP 工具集成（McpServersScreen UI + 自动注入开关）

## 新增的系统权限
为支持桌宠悬浮窗、QQ 桥接、多账号等功能，应用需要以下权限（首次启动会询问）：
- 显示在其他应用上层（SYSTEM_ALERT_WINDOW）— 桌宠悬浮窗
- 麦克风（RECORD_AUDIO）— 语音输入
- WiFi/网络状态 — 多设备发现
- 外部存储读写（旧版本兼容）

## 已知问题
- 首次安装 Live2D 模型需要联网下载（约 5MB），请在 WiFi 环境下完成
- 极少数情况下桌宠气泡显示延迟
- MCP 工具调用结果暂未流式展示（计划中）
- 实时语音对话打断（计划中）

## 反馈渠道
内测期间如遇问题，请反馈给我们。1-2 周内收集反馈后会发布正式版本。

---

## 1.3 公测版本（保留作为历史记录）
本次 1.3 安装包签名与旧版本不同，无法直接覆盖安装。请务必先在旧版本中导出完整备份，再卸载旧版本并安装 1.3，最后导入备份恢复数据。未提前备份可能导致角色、聊天记录、记忆和设置丢失。

本次更新重点新增 Live2D 桌面宠物与 QQ Bridge，并优化应用内更新、后台消息和整体稳定性。

1. 新增 Live2D 桌面宠物
- 支持桌面悬浮窗和 Live2D 模型预览
- 可绑定现有角色进行桌面互动
- 复用角色设定、聊天记录和长期记忆
- AI 回复可显示为桌宠气泡
- 后台主动消息可同步到桌宠
- 支持开机恢复、位置与显示配置

2. 新增桌宠情绪与表情系统
- 支持开心、难过、生气、惊讶、害羞和平静等情绪
- AI 可控制 Live2D 表情参数
- AI 未返回表情时自动进行本地情绪推断
- 情绪会随对话累积并随时间自然衰减

3. 新增桌宠语音回复
- 优先复用应用内 TTS 模型
- 远程 TTS 不可用时回退 Android 系统 TTS
- 支持音量控制、停止播放和临时文件清理

4. 新增 QQ Bridge（OneBot 桥接）
- 通过 NapCat 接入本地 QQ 个人号
- 支持接收群消息、好友消息并回复
- 支持图片、表情包、合并转发

5. 改进应用内更新
- APK 下载进度显示
- 自动校验 sha256
- 失败重试机制

6. 后台消息稳定性
- Android 15/16 兼容
- 开机自启动更可靠

## 历史版本
- 1.2：基础聊天、角色、多 API、记忆等核心能力
